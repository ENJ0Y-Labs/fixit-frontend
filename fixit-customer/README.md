# FixIt Customer Frontend

React + Vite customer-facing frontend prototype for FixIt.

## Run

```bash
npm install
npm run dev
```

## Validate

```bash
npm run lint
npm run build
```

The app uses a small client-side page switcher in `src/App.jsx` until routing is added.
